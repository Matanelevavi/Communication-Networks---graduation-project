# WeatherWear — מסמך שינויים

> מסמך זה נכתב אחרי מעבר מלא על הקוד ועל מסמך המיפוי `WeatherWear_מיפוי_ותיעוד_מלא.docx`.
> הוא מחליף את **חלק E** של מסמך המיפוי: כל 13 החולשות שתועדו שם כ"אנחנו יודעים עליהן"
> תוקנו או שיש להן החלטה מודעת מנומקת. בנוסף מתועדים כאן באגים שהמסמך המקורי **לא** תפס.

**מצב הבדיקות:** 237 בדיקות יחידה עוברות ב-0.24 שניות (לפני: 28, וסוויטת הבדיקות בכלל לא הייתה ניתנת להרצה).
**בדיקת קצה־לקצה:** 25 בדיקות על תהליכים אמיתיים — הרצה תקינה, נפילת שרת ראשי, 20% אובדן חבילות, שני לקוחות במקביל, ובקרת זרימה מול מקבל איטי.

---

## חלק 1 — 13 הנקודות מחלק E

### E.1 — דליפת IP ברגע ה-OFFER, בלי lease timeout · **תוקן**

**מה היה:** `pool_ip.pop(0)` ו-`used_ips[cl_add] = ip` קרו בשליחת ה-OFFER. אם ה-REQUEST או ה-ACK אבדו, הכתובת ננעלה לנצח.

**מה יש עכשיו** — שלושה מבני נתונים במקום אחד, בדיוק כמו DHCP אמיתי:

| מבנה ב-`AddressPool` | משמעות | מתי משתחרר |
|---|---|---|
| `free` | כתובות פנויות | — |
| `reserved` | הכתובת **שמורה** אחרי OFFER, עוד לא מוקצית | אוטומטית אחרי `OFFER_HOLD_TIME` (5 שניות) אם לא הגיע REQUEST |
| `leased` | הכתובת **מוחכרת** אחרי ACK | ב-RELEASE, או אוטומטית אחרי `LEASE_TIME` (300 שניות) |
| `declined` | הלקוח הודיע שבחר שרת אחר | אחרי `OFFER_HOLD_TIME` |

המעבר `free → reserved → leased` הוא בדיוק ההבדל בין "הבטחה" ל"הקצאה". `reclaim_expired()` רץ בכל סבב של לולאת השרת — הסוקט מקבל `settimeout(LEASE_SWEEP_INTERVAL)` כדי שהלולאה תתעורר גם כשאין תעבורה, ותספיק לנקות.

**התשובה בהגנה לשאלה "מה אם 50 לקוחות מתנתקים באמצע DORA?"**
כל הצעה חוזרת למאגר אחרי 5 שניות, וכל חכירה שלא שוחררה חוזרת אחרי 5 דקות. המאגר לא יכול להתרוקן מלקוחות שנעלמו.

**איפה בקוד:** `src/servers/address_pool.py` — `reserve`, `commit`, `release`, `withdraw`, `reclaim_expired`. השרת עצמו (`dhcp_server.py`) רק מדבר ברשת.

---

### E.2 — דליפה שיטתית של מאגר הגיבוי בכל DORA מוצלח · **תוקן**

זו הייתה הנקודה החמורה ביותר: כל התחברות מוצלחת מול הראשי הדליפה כתובת אחת מהגיבוי, כך שדווקא כשהראשי היה נופל — הגיבוי כבר היה ריק.

**הפתרון — "server identifier", בדיוק כמו ב-RFC 2131.** בתקן, ה-REQUEST נשלח ב-broadcast וכולל שדה שמזהה איזה שרת נבחר; כל השרתים רואים אותו, והשרת שלא נבחר משחרר את ההצעה שלו. אצלנו ה-REQUEST הוא unicast, אז מימשנו את אותו רעיון במפורש:

```python
# dhcp_client.py — ההודעה כוללת את הפורט של השרת שנבחר, ונשלחת לשני השרתים
request = f"{REQUEST_MSG}:{offered_ip}:{server_addr[1]}"
self.broadcast(request)

# dhcp_server.py — שרת שרואה שלא נבחר
if chosen_port is not None and chosen_port != self.address[1]:
    self.decline(client, chosen_port)     # רושם, ומשחרר את ההזמנה
    return
```

הרישום ב-`AddressPool.declined` נדרש בגלל התזמון: הגיבוי עונה בהשהיה של 2 שניות, כך שכשה-REQUEST מגיע אליו הוא **עדיין לא הציע כלום**. בלי הרישום הזה הוא היה מתעורר אחרי 2 שניות ומציע כתובת שאיש לא יבקש.

**התוצאה בפועל, מהלוג של הרצת קצה־לקצה אמיתית:**
```
22:53:43 -BACKUP DHCP- Backup DHCP listening on ('127.0.0.1', 8069) with 51 addresses
22:53:45 -BACKUP DHCP- Not offering to ('127.0.0.1', 8068), it already accepted another server
```
הגיבוי לא רק שלא מדליף — הוא כבר לא מוציא כתובת מהמאגר מלכתחילה. המאגר נשאר 51 כתובות.

---

### E.3 — אין retry ל-DISCOVER/REQUEST בצד הלקוח · **תוקן**

**מה היה:** כל הודעה נשלחה פעם אחת. ACK אחד שאבד = כישלון מוחלט ו-`sys.exit(1)`.

**מה יש עכשיו:** לולאת retry על שני השלבים בנפרד (`DHCP_MAX_RETRIES = 3`), כל אחת עם timeout משלה. חלון ההמתנה ל-OFFER הוא `BACKUP_OFFER_DELAY + DHCP_RETRY_TIMEOUT` = 3.5 שניות — רחב מספיק שניסיון **אחד** כבר מכסה גם failover מלא לגיבוי.

**נקודה חשובה שנפתחה בעקבות התיקון:** ברגע שהלקוח שולח DISCOVER שוב, שרת נאיבי היה מוציא כתובת שנייה מהמאגר. לכן `send_offer` הפך ל-**אידמפוטנטי**: DISCOVER חוזר מאותו לקוח מקבל את אותה הצעה בדיוק, בלי לצרוך כתובת נוספת. יש על זה בדיקה ייעודית.

> אי-העקביות שהמסמך תיאר ("RUDP מטפל באובדן, DHCP לא") כבר לא קיימת. שתי השכבות מטפלות באובדן.

---

### E.4 — אסימטריית נעילה ב-FileAgent · **תוקן**

כל גישה לקובץ עוברת עכשיו דרך מחלקה אחת, `FileStore`, שמחזיקה `threading.RLock` אחד. קריאה, כתיבה ורשימת קבצים — כולן תחת אותו מנעול. עכשיו הטענה במצגת ("הגישה למטמון מאובטחת באמצעות מנעולים") נכונה לשני הכיוונים, ואי אפשר לשכוח לנעול כי אין דרך אחרת להגיע לקובץ.

---

### E.5 — כתובת השולח של ACK לא נבדקה · **תוקן**

```python
ack_data, src = self.sock.recvfrom(1024)
if src != addr:
    log.warning(f"Ignoring ACK from unexpected source {src}")
    continue
```

נוסף גם בצד הלקוח: כל ה-chunks של העברה חייבים להגיע מאותה כתובת (ה-socket האפמרי של השרת), וכל מה שמגיע ממקור אחר נזרק. יש בדיקה שמזריקה ACK מזויף ומוודאת שהחלון לא זז בגללו.

---

### E.6 — ה-Checksum כיסה רק payload · **תוקן**

**מה היה:** `chk = zlib.crc32(payload)` — שיבוש ספרה ב-SEQ או ב-TOTAL לא נתפס.

**מה יש עכשיו:** ה-CRC מחושב על שדות הכותרת **יחד עם** ה-payload. פורמט החוט לא השתנה בכלל:

```python
# src/rudp/packet.py — הגדרה אחת שגם השולח וגם המקבל עובדים לפיה
@staticmethod
def checksum(seq, total, payload):
    return zlib.crc32(RudpPacket._fields(seq, total) + payload) & 0xFFFFFFFF

def to_bytes(self):
    chk = self.checksum(self.seq, self.total, self.payload)
    return (self._fields(self.seq, self.total)
            + f"CHK:{chk}|".encode(ENCODING) + self.payload)
```

הצד המקבל מחשב מחדש מ-seq ו-total שהוא פענח, כך שאם אחד מהם שובש — ה-CRC לא יתאים והחבילה תיזרק.

**התשובה החדשה לשאלה הקשה של כרטיס 3:** *"מה קורה אם ה-SEQ עצמו מתעוות?"* → נתפס. ה-checksum מכסה את הכותרת. יש בדיקה שמשנה `SEQ:2` ל-`SEQ:3` בחבילה תקינה ומוודאת שהיא נדחית.

---

### E.7 — פורט לקוח: תיעוד מול קוד · **תוקן**

`bind((HOST, 0))` הוחלף ב-bind ל-`CLIENT_ADD` (8068) ב-`NetworkClient`, עם נפילה מסודרת לפורט אפמרי אם 8068 תפוס:

```python
try:
    self.control_socket.bind(CLIENT_ADD)
except OSError:
    self.control_socket.bind((HOST, 0))
    log.warning(f"Port {CLIENT_PORT} is busy, using ephemeral port ... instead")
```

עכשיו טבלת הפורטים שהגשתם נכונה: ב-Wireshark תראה פורט מקור 8068. ועדיין אפשר להריץ לקוח שני להדגמת ריבוי לקוחות — הוא פשוט יקבל פורט אחר ויכתוב את זה בלוג. `CLIENT_ADD` כבר לא קבוע מת ב-config.

---

### E.8 — אין הגבלת גודל · **תוקן**

| קבוע | ערך | תפקיד |
|---|---|---|
| `MAX_REQUEST_SIZE` | 64 KB | גודל מרבי של בקשה (גם TCP וגם UDP) |
| `MAX_RESPONSE_SIZE` | 512 KB | גודל מרבי של תשובה בהעברה אחת |
| `RUDP_MAX_TRANSFER_TIME` | 60 שניות | חסם קשיח על משך העברה |

תשובה חורגת מוחלפת בהודעת שגיאה קריאה במקום להתחיל העברה אינסופית.

---

### E.9 — קריאת קובץ בינארי במצב טקסט · **תוקן**

`FileStore.read_bytes` קורא תמיד במצב בינארי, ו-`FileArchive.read` מנסה לפענח ל-UTF-8; אם זה נכשל הוא מחזיר bytes. ה-GUI מציג "Binary file, N bytes" במקום לזרוק חריגה.

---

### E.10 — `active_transfers` בלי נעילה · **תוקן**

`threading.Lock` מפורש ב-`RudpListener`. כבר לא הסתמכות על ה-GIL, ואפשר לענות "כן, יש נעילה" בלי הסתייגות.

---

### E.11 — אין ענף שמפרש דחייה מ-DHCP · **תוקן**

נוסף סוג הודעה `DHCP_NAK`. השרת שולח אותו כשהמאגר ריק וכשמתקבל REQUEST לכתובת שלא הוצעה. הלקוח מזהה אותו במפורש:

- NAK משני השרתים → כישלון מיידי עם סיבה מפורשת, בלי לחכות ל-timeout כללי
- NAK על REQUEST → הפסקה מסודרת של הניסיון

עכשיו מה שכתוב ב-PDF ההסברים ("הלקוח קלט את הדחייה") מדויק גם ברמת המנגנון, לא רק בתוצאה.

---

### E.12 — DNS ו-App Server הם נקודת כשל יחידה · **החלטה מודעת, לא שונה**

**למה לא:** המטלה לא דרשה Redundancy בכלל. גיבוי ל-DHCP הוא בונוס. הכפלת שרת ה-DNS או שרת האפליקציה הייתה דורשת מנגנון בחירת מנהיג או Load Balancer — שכבת מורכבות שלא תורמת לאף אחד מקריטריוני הניקוד.

**איך לענות:** "בחרנו להדגים Redundancy בשכבה אחת ולעשות את זה נכון, כולל טיפול בכל תרחישי הכשל שלה, במקום לפזר חצי-מימושים על שלוש שכבות. שרת האפליקציה היה מקבל Load Balancer, לא שרת גיבוי סימטרי — כי שם הבעיה היא עומס, לא זמינות."

---

### E.13 — אין הצפנה · **החלטה מודעת, לא שונה**

TLS מעל ה-RUDP שלנו היה דורש מימוש handshake מלא ידנית. זה מחוץ להיקף המטלה, וכבר מופיע במצגת שלכם כשיפור עתידי. **שיפור אחד כן נעשה:** מפתח ה-Gemini עבר מ-query string ל-header `x-goog-api-key`, כך שהוא לא נכנס ללוגים של פרוקסי ולא נראה בלכידת Wireshark של ה-URL.

---

## חלק 2 — באגים שמסמך המיפוי לא תפס

### 2.1 · חמור · סוויטת הבדיקות בכלל לא רצה

```
$ python -m unittest discover -s tests
ImportError: Start directory is not importable
```
חסר היה `tests/__init__.py`. הפקודה שרשומה בקובץ `data/Running` שלכם פשוט נכשלה. **תוקן** — הקובץ נוסף.

### 2.2 · חמור · סופת שידורים חוזרים ב-RUDP

הלולאה הראשית של `reliable_send` שידרה מחדש את **כל** החלון בכל סבב, גם כשכלום לא אבד:

```python
while unacked and retry_count < MAX_RETRIES:
    for seq in range(wind_start, wind_end):
        if seq in unacked:
            self.sock.sendto(...)     # ← נשלח שוב בכל סבב
    ack_data, _ = self.sock.recvfrom(1024)   # ← ACK אחד בלבד לכל סבב
```

עם חלון של N חבילות, החבילה האחרונה נשלחה עד N פעמים. תעבורה ריבועית שהפרוטוקול יצר על עצמו — ובלי שום אובדן אמיתי.

**התיקון:** קבוצת `in_flight` עוקבת אחרי מה שכבר בדרך; שידור חוזר קורה רק ב-timeout או ב-Fast Retransmit. יש בדיקה שמוודאת שהעברה של 10 chunks ברשת תקינה שולחת בדיוק 10 חבילות ו-0 שידורים חוזרים.

### 2.3 · חמור · רק ACK אחד נקרא בכל סבב

`recvfrom` נקרא פעם אחת לכל סבב לולאה, כך שגם כשכל ה-ACK-ים כבר המתינו ב-buffer, החלון זחל קדימה. **התיקון:** `collect_acks` קורא ACK אחד בהמתנה חוסמת ואז **מרוקן** את כל השאר ב-non-blocking. זה מה שמאפשר לחלון באמת להיפתח.

### 2.4 · חמור · דליפת `active_transfers` וחסימת לקוח לצמיתות

```python
self.active_transfers.add(addr)        # ← נוסף לפני הפענוח
payld = json.loads(msg.split("|")[1])  # ← אם זה נכשל...
```
כל בקשה פגומה השאירה את הלקוח ברשימה לנצח — הוא לא יכול היה לבצע יותר אף בקשת RUDP עד אתחול השרת. **התיקון:** מפענחים קודם, מוסיפים רק אחרי הצלחה, ויש בדיקה ייעודית.

### 2.5 · חמור · קיטום תשובות ב-TCP

צד לקוח: `response = tcp_sock.recv(BUFF_SIZE*8)` — קריאה **אחת**. צד שרת: `data = client_sock.recv(4096)` — קריאה **אחת**.
TCP הוא זרם בתים בלי גבולות הודעה; אין שום ערובה שהודעה שלמה תגיע בקריאה אחת. הורדת ה-CSV שלכם היא 5.7KB — קרוב מסוכן לגבול 8KB, וכל קובץ גדול יותר היה נחתך **בשקט**.

**התיקון:** הלקוח קורא בלולאה עד סגירת החיבור (EOF הוא מה שמגדיר את סוף ההודעה); השרת קורא בלולאה עד ש-JSON תקין מתקבל.

> **זו נקודה מצוינת להגנה:** "TCP נותן לנו אמינות, אבל **לא** גבולות הודעה. את המסגור היינו צריכים לממש בעצמנו — בדיוק ההבדל בין stream-oriented ל-message-oriented."

### 2.6 · בינוני · הודעות שגיאה נשמרו כתחזית במטמון

```python
advice = self.logic.get_ai_recommendation(...)
self.agent.save_text_file(f"{safe_city}_forecast.txt", advice)   # גם אם advice = "AI Error: quota exceeded"
```
תקלה רגעית ב-Gemini הייתה נשמרת לדיסק ומוגשת כ"תחזית" לכל בקשה **עד סוף היום** — כי המטמון מבוסס תאריך קובץ.

**התיקון:** `get_ai_recommendation` מחזיר `{"ok": bool, "text": str}`. רק תשובה עם `ok=True` נכתבת לדיסק.

### 2.7 · בינוני · קריסה על בקשה בלי עיר

`city.replace(" ", "_")` על `None` → `AttributeError` בתוך ה-thread. **תוקן** בבדיקת קלט מפורשת.

### 2.8 · בינוני · השהיה קשיחה של 2 שניות בכל העברה

```python
if seq == 2 and retry_count == 0:
    time.sleep(2)
```
קוד הדגמה שנשאר בקוד הפעיל והוסיף 2 שניות לכל העברה עם 2+ chunks. **תוקן:** עבר ל-`SIMULATE_DELAY` ב-config, כבוי כברירת מחדל, עם `DELAY_SEQ` ו-`DELAY_SECONDS` נשלטים. סוויטת הבדיקות ירדה מ-2.06 שניות ל-0.15 שניות.

### 2.9 · בינוני · תגיות הלוג היו שקריות

כל מודול קרא ל-`logging.basicConfig` בזמן import. `basicConfig` פועל **רק בפעם הראשונה** — כך שברגע שכמה מודולים נטענו יחד, כולם קיבלו את התגית של הראשון. בהרצת הבדיקות המקורית **כל** השורות הודפסו עם `-AGENT-`, כולל לוגים של DHCP, DNS והלקוח.

**התיקון:** כל מודול משתמש ב-`logging.getLogger(__name__)`, וההגדרה הגלובלית עברה ל-`setup_logging(tag)` שנקראת רק מבלוק `__main__`.

### 2.10 · בינוני · `run.py` הריץ את המפרש הלא נכון

`subprocess.Popen(["python", ...])` — לוקח את מה שנמצא ראשון ב-PATH, שזה לא בהכרח ה-venv של הפרויקט (במחשב הזה `python` הוא Anaconda, לא ה-venv). כמו כן לא הועברה `cwd`, כך שהרצה מתיקייה אחרת נשברה.

**התיקון:** `sys.executable` + `cwd=BASE_DIR`.

### 2.11 · בינוני · `sleep(2)` עיוור בהמתנה לשרתים

הוחלף בבדיקת מוכנות אמיתית: `run.py` מנסה לעשות bind לכל פורט; פורט שתפוס = השרת עלה. הלקוח מופעל רק כשכל ארבעת השרתים באמת מאזינים.

**התשובה החדשה לשאלה "מה אם שרת איטי להעלות?"** → אין יותר הנחה. יש polling עם timeout של 15 שניות ואזהרה מפורשת אם שרת לא הצליח.

בנוסף `run.py` נשאר חי; Ctrl+C סוגר את כל התהליכים. (הקובץ `data/Running` עם ה-`taskkill` כבר לא נחוץ — הוסר, גם כי הוא הופיע בארכיון ה-FTP.)

### 2.12 · בינוני · כפילות קוד מלאה בין שני שרתי ה-DHCP

`dhcp_backup.py` היה עותק כמעט מדויק של `dhcp_server.py`. **התיקון:** `DHCPBackupServer` יורש מ-`DHCPServer` ומעביר שלושה פרמטרים בלבד (כתובת, מאגר, השהיה). כל תיקון עתידי חל אוטומטית על שניהם — וזו בדיוק הסיבה שהמאגר של הגיבוי כבר לא יכול "לפגר" מאחורי הראשי.

### 2.13 · בינוני · `sleep(2)` חוסם ב-DHCP הגיבוי

הלולאה כולה נחסמה ל-2 שניות בכל DISCOVER, כך שלקוח שני המתין 4 שניות. **התיקון:** `threading.Timer`. הלולאה ממשיכה לענות מיד; רק שליחת ההצעה נדחית.

### 2.14 · נמוך · באגים קטנים

| מה | הבעיה | התיקון |
|---|---|---|
| `msg.split("|")[1]` | JSON שמכיל `\|` נחתך | `split("\|", 1)` |
| `msg.startswith("SEQ:1")` | תפס גם `SEQ:15` | `startswith("SEQ:1\|")` |
| `line.startswith("202")` | מפרסר CSV נשבר ב-2030 | בדיקת תבנית תאריך |
| `os.path.getsize` על תיקייה | תיקיות הופיעו כקבצים בארכיון | סינון `os.path.isfile` |
| `line.split(' ')[1]` ב-GUI | שם קובץ עם רווח נחתך | פיצול על `" (Size:"` |
| URL בנוי ב-f-string | "Tel Aviv" לא מקודד | `params=` של requests |
| `except Exception: pass` ב-`close()` | בלע כל שגיאה בשקט | תפיסת `OSError` + לוג |
| אין try/except על bind ב-DHCP | קריסה עם traceback | הודעה ברורה + `sys.exit(1)`, כמו ב-DNS |
| חיפוש DNS תלוי רישיות | `WeatherWear.local` נכשל | `.lower()` בשני הצדדים |
| אין `requirements.txt`/`.gitignore` | 44 קבצי `.pyc` ו-`.idea/` ב-git | נוספו; הקבצים הוסרו מהאינדקס |

---

## חלק 3 — ארכיטקטורה: פיצול לקבצים ועקרונות OOP

התיקונים בחלקים 1 ו-2 הגדילו את הקוד, ושני קבצים הפכו למפלצות: `client.py` הגיע ל-452 שורות ועשה ארבעה דברים שונים, ו-`reliable_send` הפכה לפונקציה בת 92 שורות. הפעולה הזו פירקה את המערכת למודולים שלכל אחד יש **אחריות אחת**.

**התוצאה במספרים:** 11 קבצי מקור הפכו ל-33, אף קובץ לא עובר 193 שורות, והמודול הממוצע הוא 95 שורות.

### 3.1 · `src/rudp/` — הפרוטוקול יצא מתוך השרת

**הבעיה שזה פותר היא אמיתית, לא אסתטית.** קודם `rudp.py` ישב בתוך `src/servers/app_server/`, אבל **שני** צידי כל העברה תלויים בפורמט החוט. בפועל זה אומר שהיו **שתי** הגדרות נפרדות של הפרוטוקול:

```python
# בשרת (rudp.py) — בונה כותרת
header = f"SEQ:{seq}|TOTAL:{total_chunks}|CHK:{chk}|".encode('utf-8')

# בלקוח (client.py) — מפרק כותרת, ידנית, בנפרד לגמרי
parts = data.split(b'|', 3)
seq = int(parts[0].split(b':')[1])
```

שתי הגדרות של אותו דבר יכולות להיפרד בשקט: מספיק שמישהו ישנה שדה בצד אחד. עכשיו יש הגדרה **אחת**, `RudpPacket`, ושני הצדדים עוברים דרכה.

| מודול | אחריות | שורות |
|---|---|---|
| `packet.py` | פורמט החוט וה-checksum — מקור אמת יחיד | 115 |
| `congestion.py` | חלון העומסים, כאריתמטיקה טהורה | 62 |
| `window.py` | מצב העברה אחת: מה ממתין, מה באוויר, מה נשלח | 79 |
| `sender.py` | השולח עם החלון המחליק | 175 |
| `receiver.py` | הרכבה מחדש ו-ACK | 130 |

`congestion.py` ו-`window.py` הם המקרה הכי ברור: אלה כללי בקרת העומסים והחלון, שני נושאים שאפשר לבדוק **בלי socket בכלל**. 16 בדיקות רצות עליהם ישירות.

### 3.2 · `Transport` — פולימורפיזם שהוא בדיוק הנושא של המטלה

```python
class Transport(ABC):
    @abstractmethod
    def request(self, payload: str) -> str | bytes | None: ...

class TcpTransport(Transport):   # מערכת ההפעלה נותנת אמינות; אנחנו רק ממסגרים
class RudpTransport(Transport):  # שום דבר לא בחינם; הכל ב-src/rudp
```

זו לא הפשטה לשם ההפשטה. שום קוד מעל השכבה הזו לא מסתעף על "TCP או RUDP" — `session.py` פשוט קורא ל-`client.request(payload, protocol)`. **ההבדל בין שני הפרוטוקולים, שהוא כל הפרויקט, מרוכז בקובץ אחד של 141 שורות.**

זו גם תשובה מוכנה אם ישאלו "איך הייתם מוסיפים QUIC": עוד מחלקה שיורשת מ-`Transport`, בלי לגעת בשום דבר אחר.

### 3.3 · `AddressPool` — הכללים המעניינים יצאו מלולאת ה-socket

`dhcp_server.py` החזיק גם את מכונת המצבים של DORA וגם את כל ניהול הכתובות. הפרדנו:

- **`AddressPool`** — free / reserved / leased / declined, המעברים ביניהם והפקיעה שלהם. 17 בדיקות, אפס sockets.
- **`DHCPServer`** — מקבל datagram, מפענח, קורא לשיטה המתאימה ב-pool, עונה.

התיקונים של E.1 ו-E.2 חיים עכשיו במחלקה אחת שאפשר להוכיח שהיא נכונה בבדיקות, במקום להיות פזורים בלולאה.

### 3.4 · שרת האפליקציה — נתב אחד, שני מאזינים

`app.py` היה 268 שורות עם שלוש אחריויות. עכשיו:

| מודול | אחריות | שורות |
|---|---|---|
| `router.py` | בקשה → תשובה. **לא יודע איך הבקשה הגיעה** | 86 |
| `tcp_listener.py` | TCP, והמסגור ש-TCP לא נותן | 104 |
| `rudp_listener.py` | handshake, transfers, ניהול `active_transfers` | 139 |
| `app.py` | מרכיב את השלושה ומריץ | 45 |

`RequestRouter` מקבל את שלושת התלויות שלו ב-constructor (`weather`, `advisor`, `agent`), כך שבדיקה יכולה להחליף כל אחד מהם — 12 בדיקות רצות עליו בלי רשת ובלי דיסק.

### 3.5 · שירותים חיצוניים — חריגות במקום מחרוזות שגיאה

`logic.py` פוצל ל-`WeatherService` ו-`AiAdvisor`, שיורשים מ-`ExternalService`. השינוי החשוב הוא לא הפיצול אלא **טיפול השגיאות**:

```python
# קודם: השגיאה חוזרת כמחרוזת, ואי אפשר להבדיל בינה לבין תשובה אמיתית
advice = self.logic.get_ai_recommendation(...)
self.agent.save_text_file(..., advice)      # ← נשמר גם "AI Error: quota exceeded"

# עכשיו: כישלון הוא חריגה, ולכן הקוד פשוט לא מגיע לשמירה
forecast = self.weather.fetch(city)
advice = self.advisor.recommend(forecast, profiles)
self.agent.store_forecast(safe_city, advice)   # ← מגיעים לכאן רק בהצלחה
```

**הכלל "לא לשמור שגיאות במטמון" (באג 2.6) הפך ממשהו שצריך לזכור לבדוק, למשהו שזרימת הבקרה אוכפת.** אי אפשר לשמור שגיאה בטעות, כי במסלול הכישלון פשוט אין קריאת שמירה.

בנוסף, `Forecast` ו-`Place` הם `dataclass` במקום מילונים — `forecast.min_temp` במקום `weather_data["min_temp"]`, עם שגיאה בזמן פיתוח אם מקלידים שם שדה לא נכון.

### 3.6 · `FileAgent` — שלוש שכבות במקום אחת

| מודול | אחריות |
|---|---|
| `storage.py` | `FileStore`: התיקייה, המנעול, ובדיקת ההכלה. **כל** גישה לקובץ עוברת כאן |
| `forecast_cache.py` | `ForecastCache`: תחזית אחת לעיר ליום |
| `file_archive.py` | `FileArchive`: רשימה, קריאה, ועיצוב CSV לטבלה |
| `agent.py` | `FileAgent`: מרכיב את השניים, ומדבר במונחי עיר ולא במונחי שם קובץ |

ההגנה מפני path traversal והנעילה חיים עכשיו **במקום אחד**. קודם כל פונקציה הייתה צריכה לזכור לעשות `basename` ולנעול — ושתיים מהן שכחו (זה היה E.4 ו-E.9).

### 3.7 · ה-GUI — סוף ל-dict בתוך closure

```python
# קודם: התוצאה מוברחת החוצה דרך מילון שנתפס ב-closure
def get_user_data_gui():
    user_data = {"action": None, ...}
    def submit():
        user_data["action"] = action_var.get()   # ← כתיבה למשתנה חיצוני
    ...
    return (user_data["action"], ...) if user_data["action"] else (None,)*4

# עכשיו: המסך הוא אובייקט, והתוצאה היא תכונה שלו
class RequestWindow(BaseWindow):
    def submit(self):
        self.result = (action, city, profiles, protocol)
        self.close()
```

`BaseWindow` מחזיקה את מה שכל שלושת המסכים חלקו (יצירת חלון, כותרת, כפתור, סגירה), ו-`theme.py` מחזיקה את הצבעים והפונטים שהיו מפוזרים כ-literals — `#eef2f5` הופיע שש פעמים.

### 3.8 · שיפורים רוחביים

- **Type hints** על כל שיטה ציבורית בכל המודולים.
- **Docstrings** שמסבירים *למה*, לא *מה* — ובמיוחד למה משהו נכתב כך ולא אחרת.
- **`from src.config import *` הוחלף בייבוא מפורש** בכל מודול. עכשיו רואים מה כל קובץ באמת צריך, ו-IDE יכול לזהות שם שגוי.
- **קבועים במקום literals**: `HANDSHAKE_PREFIX`, `FIN_MSG`, `RUDP_LINGER`, `TABLE_WIDTH` וכו'.

---

## חלק 4 — שיפורים שמחזקים תשובות לשאלות הקשות

### 4.1 · Slow Start אמיתי עם ssthresh

**כרטיס G.4 שאל:** *"יש כאן Slow Start אמיתי בשני שלבים כמו ב-TCP?"* — התשובה הישנה הייתה "לא, אין ssthresh". **עכשיו יש:**

```python
def on_ack(self):
    if self.cwnd < self.ssthresh:
        self.cwnd += 1.0                 # Slow Start: הכפלה בכל RTT
    else:
        self.cwnd += 1.0 / self.cwnd     # Congestion Avoidance: +1 בכל RTT

def on_timeout(self):
    self.ssthresh = max(self.cwnd / 2.0, 1.0)
    self.cwnd = 1.0                      # timeout = עומס חמור, חוזרים ל-Slow Start

def on_triple_dup_ack(self):
    self.ssthresh = max(self.cwnd / 2.0, 1.0)
    self.cwnd = self.ssthresh            # חוצים, לא מקריסים
```

זו בדיוק ההתנהגות של TCP Reno: timeout מקריס ל-1, שלושה ACK כפולים רק חוצים. יש 4 בדיקות שמכסות את שלושת המעברים ואת החסם העליון.

### 4.2 · TTL אמיתי מהשרת ב-DNS

**כרטיס G.2 שאל:** *"מאיפה מגיע ה-TTL, מהשרת או מהלקוח?"* — התשובה הישנה הייתה "מקבוע מקומי בלקוח, בניגוד ל-DNS תקני".

**עכשיו:** תשובת השרת היא `RESOLVED:<ip>:<ttl>`, והלקוח שומר במטמון לפי ה-TTL שקיבל. `DNS_CACHE_TTL` נשאר כערך גיבוי בלבד למקרה ששרת ישן עונה בלי TTL. זה בדיוק המודל של DNS אמיתי: השרת האותוריטטיבי קובע את מדיניות ה-cache.

### 4.3 · מדדים גלויים ב-RUDP

כל העברה מסתיימת בשורת לוג עם מספר החבילות ומספר השידורים החוזרים:
```
Transfer to ('127.0.0.1', 51168) complete: 8 packets sent, 0 retransmissions
Transfer to ('127.0.0.1', 52418) complete: 8 packets sent, 2 retransmissions
```
זה מספר בעצמו את סיפור בקרת העומסים בהדגמה, בלי לחפש ב-Wireshark.

---

### 4.4 · Flow Control — הרכיב האחרון מרשימת המטלה

המטלה מנתה שלושה רכיבים ל-RUDP: Reliability, Congestion control ו-**Flow control**. השניים הראשונים היו קיימים; השלישי היה חסר לגמרי, וזו הייתה הנקודה שהמסמך תיאר במפורש כ"לא מומש".

**למה זה לא היה טריוויאלי להוסיף.** המקבל צבר את **כל** ה-chunks במילון עד לסיום ההעברה. מבנה כזה לא יכול להתמלא בצורה משמעותית ואין לו מה לפרסם — אין "מקום פנוי" שמשתנה. כדי ש-Flow Control יהיה אמיתי ולא קישוט, המקבל היה צריך קודם להתנהג כמו מקבל TCP:

```python
seq == next_expected  ->  נמסר מיד, לא תופס buffer
seq >  next_expected  ->  מוחזק, תופס משבצת, עלול להידחות
seq <  next_expected  ->  כבר נמסר, כפילות
```

עכשיו `ReceiveBuffer` מוסר בתים לפי הסדר מיד ומשחרר את הזיכרון, ומחזיק **רק** chunks שהגיעו מוקדם מדי. המקום שנשאר ב-buffer הזה הוא בדיוק ה-`rwnd` שמפורסם.

**שינוי הפרוטוקול הוא שדה אחד:**

```
ACK:<n>              ← קודם
ACK:<n>|WIN:<rwnd>   ← עכשיו
```

השדה אופציונלי, כך ש-ACK בלי `WIN` עדיין תקין ופשוט לא נושא פרסום.

**בצד השולח, שני חסמים בלתי תלויים והקטן מנצח:**

```python
def effective_window(self) -> int:
    limit = self.congestion.window            # מה שהרשת יכולה לשאת
    if self.peer_window is not None and self.peer_window < limit:
        limit = self.peer_window              # מה שהמקבל יכול להחזיק
    return max(1, limit)
```

ה-`max(1, ...)` הוא לא זהירות סתמית: הוא מונע **deadlock**. אם המקבל מפרסם 0, ה-chunk היחיד שישחרר את ה-buffer שלו הוא בדיוק זה שבראש החלון — ולכן חייבים לתת לחבילה אחת לעבור תמיד. זה בדיוק התפקיד של ה-persist timer ב-TCP.

**זה עובד בפועל, מלוג של הרצת קצה-לקצה עם buffer של 2 חבילות:**
```
Flow control: receiver window 2 is below cwnd 4, sending 2
Flow control: receiver window 2 is below cwnd 5, sending 2
Flow control: receiver window 1 is below cwnd 6, sending 1
Transfer complete: 8 packets sent, 2 retransmissions, 4 rounds limited by the receiver
```
חלון העומסים גדל ל-4, 5, 6 — והמקבל בלם אותו ל-2 ול-1. הקובץ הגיע זהה בתים.

**התשובה בהגנה להבדל בין השניים:** בקרת עומסים היא ניחוש של השולח לגבי **הרשת**, שנמדד מאובדן ומ-timeout. בקרת זרימה היא עובדה שהמקבל **מוסר** לשולח על עצמו. קישור מהיר לנמען איטי מוגבל על ידי השנייה; קישור איטי לנמען מהיר מוגבל על ידי הראשונה.

**איפה בקוד:** `src/rudp/receive_buffer.py` (המקבל), `src/rudp/packet.py` (השדה), `src/rudp/sender.py::effective_window` (השולח). 26 בדיקות ייעודיות.

---

## חלק 5 — מה נשאר פתוח במכוון

| נושא | למה לא מומש | איך לענות |
|---|---|---|
| **Redundancy ל-DNS/App** | ראו E.12 | שם |
| **הצפנה** | ראו E.13 | שם |
| **מטמון "יומי" ולא TTL של 24 שעות** | מכוון: "התחזית של היום" היא מושג יומי, לא חלון מתגלגל | "זה cache יומי לפי `mtime`, לא TTL. תחזית מאתמול לא רלוונטית גם אם נשמרה לפני 10 דקות." |

---

## חלק 6 — פערים בין הקוד למסמכים שהגשתם

1. **שם הדומיין:** בקוד `weatherwear.local`, במסמך המיפוי `local.weatherwear`. **הקוד נכון** (TLD בסוף, כמו בכל שם דומיין). כדאי לתקן במצגת אם מופיע שם.
2. **פורט הלקוח:** היה פער (E.7) — **נסגר לטובת המסמך**, הקוד עכשיו משתמש ב-8068.
3. **קובץ `data/Running`:** הכיל פקודות עם שמות מודולים שלא קיימים (`dhcp_server_backup`, `main_app`). הוסר; פקודות ההרצה הנכונות נמצאות ב-`README.md`.

---

## חלק 7 — איך להריץ ולהדגים

```bash
python run.py                       # הכל, עם בדיקת מוכנות
python -m src.client.client         # לקוח שני להדגמת ריבוי לקוחות
python -m unittest discover -s tests -t .    # 237 בדיקות
```

הדגמות בלי לגעת בקוד:

```bash
SIMULATE_LOSS=1 python run.py       # אובדן 20%: שידורים חוזרים והתכווצות חלון
SIMULATE_DELAY=1 python run.py      # השהיית חבילה: timeout ושחזור
MOCK_LLM=1 python run.py            # בלי מפתח API בכלל
```

ב-Windows PowerShell: `$env:SIMULATE_LOSS=1; python run.py`

---

## חלק 8 — סטטוס בדיקות

**237 בדיקות יחידה**, כולן עוברות ב-0.24 שניות, עם socket ממוקק (לא צריך שרתים רצים ושום דבר לא נוגע ברשת):

| קובץ | בדיקות | מכסה |
|---|---|---|
| `test_rudp_sender.py` | 20 | שידור-פעם-אחת, אובדן, ACK מזויף, בקרת זרימה |
| `test_address_pool.py` | 17 | הזמנות, חכירות, פקיעה, אידמפוטנטיות, "שרת אחר נבחר" |
| `test_rudp_packet.py` | 17 | מסגור, checksum על כותרת, ACK+WIN, חלוקה ל-chunks |
| `test_dhcp_server.py` | 14 | DORA מלא, NAK, DISCOVER כפול, הגיבוי, ויתור על הצעה |
| `test_listeners.py` | 14 | מסגור TCP, handshake, דליפת transfer, חריגת גודל |
| `test_dhcp_client.py` | 12 | retry לכל שלב, server identifier, NAK, שחרור |
| `test_router.py` | 12 | ניתוב, מטמון, אי-שמירת שגיאות, עיר חסרה |
| `test_rudp_receive_buffer.py` | 12 | מסירה לפי סדר, buffer מלא, כפילויות, rwnd |
| `test_weather_api.py` | 12 | קלט עברי, קידוד פרמטרים, כשלי רשת, CSV |
| `test_agent.py` | 11 | מטמון יומי, ארכיון, פרסור CSV |
| `test_storage.py` | 11 | path traversal, בינארי, סינון תיקיות ו-dotfiles |
| `test_transport.py` | 11 | factory, מסגור TCP, handshake RUDP, מקור זר |
| `test_client.py` | 10 | bind + fallback, הרכבה, teardown |
| `test_gui.py` | 10 | פרסור פרופילים, שמות קבצים עם רווח, bytes |
| `test_rudp_receiver.py` | 10 | סדר הפוך, שיבוש, כפילות, מקור זר, חלון מלא |
| `test_rudp_window.py` | 9 | הזזת חלון, dup-ACK, timeout |
| `test_ai_advisor.py` | 8 | prompt, מפתח ב-header, שגיאות כחריגות |
| `test_dns_resolver.py` | 8 | TTL מהשרת, cache תקף ופג |
| `test_rudp_congestion.py` | 7 | slow start, congestion avoidance, AIMD |
| `test_session.py` | 7 | בניית בקשה, לולאת המשתמש, מסלול FTP |
| `test_dns_server.py` | 5 | TTL בתשובה, רישיות, פורט תפוס |
| **סה"כ** | **237** | |

**בדיקת קצה־לקצה** (`23/23` עוברות) על תהליכים אמיתיים:

| תרחיש | מה נבדק |
|---|---|
| A. הרצה תקינה | DORA, DNS+TTL, FTP ב-TCP וב-RUDP, העברה רב-chunk, מטמון, שגיאות, path traversal, אי-דליפה בגיבוי |
| B. ראשי כבוי | הלקוח מקבל `192.168.2.100` מהגיבוי |
| C. אובדן 20% | העברה של 5.5KB מסתיימת נכון עם 2 שידורים חוזרים |
| D. שני לקוחות | כתובות שונות, קבצים שונים, בלי ערבוב |
| E. בקרת זרימה | buffer של 2 חבילות: ההעברה נכונה, והשרת האט בפועל |
